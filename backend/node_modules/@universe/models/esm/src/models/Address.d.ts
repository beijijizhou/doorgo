import { IAddress } from '../types/index.js';
export type Validator<T> = {
    [key in keyof T]: (v: unknown) => v is T[key];
};
export type Logger = false | ((msg: string) => void);
export declare function isAddress(o: unknown, log?: Logger): o is IAddress;
export declare function isAddressOrNull(o: unknown): o is IAddress | null;
