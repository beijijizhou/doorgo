declare const SQL_QUERY = "\nCREATE TABLE IF NOT EXISTS Score (\n  \"name\" TEXT NOT NULL,\n  \"value\" REAL NOT NULL,\n  \"updatedAt\" INTEGER,\n  \"personId\" TEXT NOT NULL REFERENCES Person(id) ON UPDATE CASCADE ON DELETE CASCADE DEFERRABLE INITIALLY DEFERRED,\n  CONSTRAINT ScorePk PRIMARY KEY (\"personId\", \"name\")\n) WITHOUT ROWID;\n\n";
export default SQL_QUERY;
