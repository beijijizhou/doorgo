import { Country, State } from '../types/enums.js';
import { Aliases } from './utils.js';
export interface StateDesc {
    name: string;
    code: State;
    country: Country;
    number: number;
}
export declare function getStateDesc(state: State): StateDesc | null;
declare const _default: Aliases<State>;
export default _default;
